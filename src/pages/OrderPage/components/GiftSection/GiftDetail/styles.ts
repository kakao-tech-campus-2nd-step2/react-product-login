import { css } from '@emotion/react';

import { colors } from '@/styles/variants/theme';

export const containerStyle = css({
  padding: '1rem',
});

export const cardStyle = css({
  padding: '1rem',
});

export const subtitleStyle = css({
  color: colors.blackOpacity[60],
});
